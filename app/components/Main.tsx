import React from 'react';
import MapComponent from "@components/MapComponent";

const Main: React.FC = () => {
    return <div>
        <MapComponent></MapComponent>
    </div>
};

export default Main;
